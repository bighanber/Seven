<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.luuu.seven">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".ComicApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:networkSecurityConfig="@xml/network_security_config"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity"/>
        <activity android:name=".module.update.ComicUpdateActivity"/>
        <activity android:name=".module.sort.ComicSortActivity"/>
        <activity android:name=".module.special.ComicSpecialActivity"/>
        <activity android:name=".module.special.detail.ComicSpecialDetailActivity"/>
        <activity android:name=".module.intro.ComicIntroActivity"/>
        <activity android:name=".module.read.ComicReadActivity"/>
        <activity android:name=".WebActivity"/>
        <activity android:name=".module.search.ComicSearchActivity"/>
        <activity
                android:name=".SplashActivity"
                android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".module.read.recycler.ComicReadRecyclerActivity"/>
        <receiver android:name=".receiver.SecretCodeReceiver">
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE"/>
                <data android:scheme="android_secret_code" android:host="77"/>
            </intent-filter>
        </receiver>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>